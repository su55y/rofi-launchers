## wikipedia search

> `BROWSER` environment variable must be set, e.g. `BROWSER=firefox`

### dependencies

- [go](https://go.dev/) (_for building helper_)

### building go helper

```bash
go build helper.go
```
